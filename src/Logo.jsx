export function LogoMark({ className = '', alt = 'Red Sun' }) {
  return <img src="/logo-mark.png" alt={alt} className={`logo-mark ${className}`} />
}

export function LogoLockup({ className = '', alt = 'Red Sun Transport' }) {
  return <img src="/logo-lockup.png" alt={alt} className={`logo-lockup ${className}`} />
}
